name: CI

on:
  # Every pull request, whatever it targets. A filter here would silently skip a pull request
  # stacked on another feature branch, and nothing reports a job that never ran: the pull request
  # shows as clean because there is no check missing.
  pull_request:
  push:
    branches: [main]

# A newer push to the same branch supersedes an in-flight run.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  verify:
    name: Lint and typecheck
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v7

      # Reads the version from the `packageManager` field in package.json.
      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v7
        with:
          node-version-file: .node-version
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm lint

      # Runs even when linting fails, so one push surfaces every problem.
      - name: Typecheck
        if: ${{ !cancelled() }}
        run: pnpm typecheck
