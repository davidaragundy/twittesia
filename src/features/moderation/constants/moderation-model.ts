// The model that screens what people write. The alias follows TypeSafe's latest stable release,
// so a new version can change the probabilities without a change here; pin a version ID to hold
// the threshold steady.
export const MODERATION_MODEL = "jev-latest";
