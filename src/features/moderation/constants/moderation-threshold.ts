// How likely a category has to be before content is hidden for it. It starts where TypeSafe's
// guardrails cookbook puts its strict action line; weigh it against real posts before moving it.
export const MODERATION_THRESHOLD = 0.7;
