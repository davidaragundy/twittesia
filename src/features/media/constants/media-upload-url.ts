// The route that authorises each upload before the browser sends the file to the bucket. Each kind
// is its own route on it, named by `?route=`.
export const MEDIA_UPLOAD_URL = "/api/media/upload";
