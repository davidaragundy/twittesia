import { handleStart } from "@/features/auth/utils/handle-start";
import { handleStartVisit } from "@/features/auth/utils/handle-start-visit";

export const GET = handleStartVisit;
export const POST = handleStart;
